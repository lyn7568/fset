  <map
    id="myMap"
    style="width: 100%; height:{{height}}px;"
    v-if="{{markers}}"
    latitude="{{latitude}}"
    longitude="{{longitude}}"
    markers="{{markers}}"
    show-location
  ></map>
